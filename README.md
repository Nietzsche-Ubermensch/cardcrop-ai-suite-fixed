# cardcrop-ai-suite-fixed
CardCrop AI Suite — trading card cropper/enhancer. Fixed: default view now shows manual controls, backend pipeline does real denoise/sharpen instead of a fake resize, batch WebGL renders are concurrency-limited.
